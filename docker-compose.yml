version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - WEATHER_API_KEY=${WEATHER_API_KEY}
      - AI_API_KEY=${AI_API_KEY}
    ports:
      - "5000:5000"